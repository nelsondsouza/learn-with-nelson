# T08 Exercise — SELECT Basics

Using the `students` table, write queries for:

1. all columns
2. only `name`
3. `name` and `city`
4. distinct cities
5. `name` renamed to `student_name`
6. first three rows using `LIMIT`

Do not copy the solution until you have tried each query.
