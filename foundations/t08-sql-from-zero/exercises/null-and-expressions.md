# T08 Exercise — NULL and Expressions

Write queries to:

1. find students with missing email
2. find students with an email
3. show `name`, `score`, and `score + 5` as `adjusted_score`
4. show each student's age next year as `next_age`

Then answer:

Why is this incorrect?

```sql
WHERE email = NULL
```
