# T08 — SELECT Example Answers

```sql
SELECT *
FROM students;

SELECT name
FROM students;

SELECT name, city
FROM students;

SELECT DISTINCT city
FROM students;

SELECT name AS student_name
FROM students;

SELECT *
FROM students
LIMIT 3;
```
