# T07 Exercise — Read the Error

Run these separately and identify the likely error.

A:

```python
print("Hello"
```

B:

```python
print(user_name)
```

C:

```python
age = int("hello")
```

D:

```python
print("Age: " + 25)
```

For each:

1. read the final error line
2. identify the exception type
3. inspect the referenced line
4. explain the problem
5. make one change
6. run again
