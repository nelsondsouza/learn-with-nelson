# T07 Exercise — First Python Program

Create:

```text
python-learning-project/
└── src/
    └── hello.py
```

Add:

```python
print("Hello, world!")
print("I am learning Python.")
```

Save and run it from the integrated terminal.

Then change the message, save, and run again.

Questions:

1. What does `print()` do?
2. Why must you save before running?
3. What does `.py` indicate?
