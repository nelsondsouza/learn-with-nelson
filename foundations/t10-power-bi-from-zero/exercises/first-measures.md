# Exercise — First Measures

Create:

```DAX
Total Sales = SUM(Sales[Total])
```

Then:

```DAX
Average Sale = AVERAGE(Sales[Total])
```

Then:

```DAX
Number of Sales = COUNTROWS(Sales)
```

Place each measure in a visual.

Change a filter or slicer and observe whether the measure result changes.

Explain why measures are dynamic calculations.
