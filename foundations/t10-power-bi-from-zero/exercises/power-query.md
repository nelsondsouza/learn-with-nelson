# Exercise — Power Query

In Power Query Editor:

1. inspect column names
2. inspect data types
3. rename the query to `Sales`
4. add a custom column named `Total`
5. use Quantity × Unit Price
6. confirm the new column is numeric
7. choose **Close & Apply**

Explain the difference between:

```text
Transforming data before load
```

and:

```text
Calculating a report measure after load
```
