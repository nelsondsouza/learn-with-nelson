# T06 — Clone and Pull Example

```text
git clone https://github.com/USERNAME/REPOSITORY.git
cd REPOSITORY
git remote -v
git log --oneline
```

After a remote commit exists:

```text
git status
git pull
git log --oneline
```

A fresh clone is already a Git repository.
