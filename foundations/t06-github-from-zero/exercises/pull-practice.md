# T06 Exercise — Pull Practice

In a safe learning repository, edit README on GitHub's web interface and commit it.

Back locally:

```text
git status
git log --oneline
git pull
git status
git log --oneline
```

Confirm the remote edit arrived locally.

Explain why checking `git status` before pulling is useful.
