# T09 — Formula Example

Relative formula:

```excel
=D2*E2
```

When filled down it becomes:

```excel
=D3*E3
```

Absolute reference example:

```excel
=G2*(1+$B$7)
```

When filled down, `G2` changes by row but `$B$7` stays fixed.
